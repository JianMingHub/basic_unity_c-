using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MyDynamic : MonoBehaviour
{
    dynamic dynamicValue;
    // Start is called before the first frame update
    void Start()
    {
        // dynamicValue = "Student";
        // dynamicValue++;
    }
}
